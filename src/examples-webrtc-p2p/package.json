{
  "name": "examples-webrtc-p2p",
  "version": "1.0.0",
  "license": "MIT",
  "private": true,
  "type": "module",
  "devDependencies": {
    "eslint": "^8.43.0",
    "eslint-config-prettier": "^8.8.0",
    "npm-run-all": "^4.1.5",
    "prettier": "^2.8.8",
    "rimraf": "^5.0.1"
  },
  "dependencies": {
    "ws": "^8.13.0"
  },
  "scripts": {
    "signaling": "node signaling/",
    "start": "node web/server.js",
    "clean": "rimraf dist/",
    "lint:prettier": "prettier . \"!test*\" --check --ignore-path ../../.gitignore",
    "lint:eslint": "eslint . --ext .js,.jsx,.mjs,.ts,.tsx,.mts --ignore-path ../../.gitignore",
    "lint": "run-p lint:*",
    "lintfix:prettier": "yarn lint:prettier --write",
    "lintfix:eslint": "yarn lint:eslint --fix",
    "lintfix": "run-s lintfix:prettier lintfix:eslint "
  }
}
